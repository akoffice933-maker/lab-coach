# HTB Challenges (docker spawn, публичный IP:port, VPN не нужен).
# cp practice/.env.htb-challenge .env
# Впиши ADMIN_IDS и SPAWNED_TARGET=<IP:port> из карточки. После сессии очисти SPAWNED_TARGET.
# Живые IP в git не коммитить.

ADMIN_IDS=123456789
LAB_PLATFORM=htb
REQUIRE_VPN=true
SPAWNED_TARGET=203.0.113.50:30976
MAX_SCANS_PER_HOUR=20

ALLOWED_LAB_CIDRS=10.10.10.0/23,10.129.0.0/16
ALLOW_LOOPBACK=false

NUCLEI_PATH=nuclei
NMAP_ENABLED=false
SCAN_TIMEOUT_SECONDS=300

OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
LLM_MODEL=openai/gpt-4o-mini
LLM_ENABLED=true
LLM_PROVIDER=openrouter
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_MODEL=qwen2.5:7b
OLLAMA_API_KEY=

HTB_DIR=~/HTB
AGENT_ROLE=coach

BOT_TOKEN=
ENVIRONMENT=lab
LOG_LEVEL=INFO
DATABASE_URL=sqlite+aiosqlite:///./data/lab.db
MCP_TRANSPORT=stdio
