# Учебные карточки (без флагов)

Формат карточки:

- класс уязвимости и куда смотреть в коде/UI;
- как защищаться;
- ссылка на публичный writeup **по желанию**;
- **нет** рабочего `exploit.py`, **нет** флага, **нет** живого IP инстанса.

Lab Coach — преподаватель, не solver. Новые карточки не содержат эксплойтов.

| Карточка | Класс | Эксплойт в репо |
|---|---|---|
| `pwn-abyss-40799` | переполнение в `cmd_login` | исторический пример; новые так не добавляем |
| `web-router-40762` | ping-обёртка + скрытый admin | нет |
